# URL de produção do site (usada em metadata, OG, sitemap.xml e robots.txt).
# Em preview deployments do Vercel isso cai automaticamente para a URL do deployment
# se essa variável não for definida (ver src/lib/site.ts).
NEXT_PUBLIC_SITE_URL=https://levaki.com.br
